extends layout.pug

block title
  title= title

block content
   form(method="post")
    p
      label(for="name") name:
      input(type="input", name="login" required="required")
    p
      label(for="pwd") Pwd:
      input(type="input", name="password" required="required")
    p.buttons
      input(type="submit", value="Submit")